# 会計人プロフィール（Accounting Profile）

会計士・税理士・経理・受験生のための**プロフィールメーカー**です。49問のLikertと7つの選択操作から、5つの独立したレイヤーで「あなたの会計人としての輪郭」を可視化します。

**ビルド不要の静的サイト**（Vanilla JS + ES Modules）。サーバーを持たず、採点も結果表示もすべてブラウザ内で完結します。

```bash
node scripts/dev-server.mjs
```

→ http://localhost:4173

---

## 結果はサーバーに保存されません

このアプリにはバックエンドがありません。**構造的に、回答も結果も外部へ送信されません。**

| 項目 | 扱い |
|---|---|
| 回答内容 | ブラウザ内でのみ保持。送信しない |
| 採点・結果生成 | すべてクライアントサイド |
| 一時保存 | `localStorage` に**回答内容のみ**（この端末内）。中断からの再開と、前回の結果の再表示に使う。結果そのものは保管しない |
| 結果の保管 | **行わない**。結果画面を閉じると同じ結果は再取得できない |
| 共有リンク | `/t/{CODE}/#p=CODE.pct.pct.pct.pct`。数値はURLフラグメントのためサーバーへ送信されない。含まれるのは Work Style 4軸とアーキタイプのみ。パス（`/t/{CODE}/`）はアーキタイプまでしか示さない |
| 外部リクエスト | Google Fonts（Shippori Mincho・明朝体のフォールバック）と、ユーザーが押した場合のみ X の共有画面。いずれも回答データは含まない |

`localStorage` に置くキーは次の2つだけです。

- `kprofile.v1` — 回答内容（`ans` / `ops` / `page`）。中断時の再開と、トップの「前回の結果をもう一度見る」に使う。トップの「この端末の回答を削除」または結果画面の「もう一度作る」で消去
- `kprofile.autoAdvance` — 自動で次ページへ進むかの設定

### 結果を残せるのは、回答直後のその場だけ

サーバーに保管しない以上、結果を他の端末へ持ち出す手段は **PNG プロフィールシート**
（5レイヤーを1枚にした縦長画像・1080×約2500 @2x）だけです。ブラウザ内で生成し、送信は行いません。

同じ端末に限っては `kprofile.v1` が残っている間、トップの「前回の結果をもう一度見る」で
結果を作り直して再表示できます（結果そのものではなく回答から再計算しています）。

保存ボタンを出すのは**自分の回答から確定した結果を表示しているときだけ**です。アーキタイプ図鑑のプレビューや共有リンクからの復元では表示しません。

> 以前は JSON でも書き出し、それを読み込んで結果を復元できるようにしていましたが、
> 「保存したい人には画像で足りる」という判断で廃止しました（`downloadJson` /
> `parseSavedFile` を削除）。中断からの再開は元々 `localStorage` の下書きが担っており、
> JSON 廃止の影響を受けません。

---

## 診断モデル

5つのレイヤーは**別系統でスコアリングし、相互に加点しません**。総合点は作らず、適職判定・求人推薦・年収予測・採用評価は行いません。

| # | レイヤー | 内容 | 設問/操作 | 出力 |
|---|---|---|---|---|
| 1 | **Personality** | Big Five（IPIP等の公開心理尺度を参考にした独自和文項目） | 25問（各特性5問） | 5特性の連続スコア 0–100 |
| 2 | **Work Style** | 会計実務の進め方（プロダクト独自4軸） | 16問（各軸4問） | 4軸の連続スコア + アーキタイプ |
| 3 | **Subject DNA** | 試験科目への興味（経験資格で科目リストを切替） | 4操作（S0–S3） | 上位3科目 + 理由タグ |
| 4 | **Practice DNA** | 実務領域への興味（経験/好き/やりたいを区別） | 3操作（P1–P3） | 上位4領域 + 理由タグ |
| 5 | **Study Behavior** | 学習行動 | 8問（各1問） | 8指標を3段（いまの学習スタイル／ときどき／これから試せる）に振り分けたタグ |

計 **49問 + 7操作 = 56回答**（18ページ）。

### 2段構成 — 41問で一度結果を出す

アーキタイプは **Work Style のみ**から生成するので、Personality と Work Style が
そろった時点でシェアできる結果が完成します。そこで出題を2段に分けています。

| | 内容 | 回答 | ページ |
|---|---|---|---|
| **コア** | Personality 25 + Work Style 16 | 41 | 9（5問×5 + 4問×4） |
| **任意** | Study Behavior 8 + Subject/Practice DNA 7操作 | 15 | 9（4問×2 + 7操作） |

コアを終えると結果画面に入り、そこに「あと15問で残り3レイヤー」の導線が出ます。
進捗の分母もコア／任意で分けており、コア中に `41/56` のような未完了表示は出しません。

未回答のレイヤーは結果画面でカードごと表示されないため、途中結果と完全版で
画面を作り分けていません。ページ境界はセクションの切れ目と一致させています
（`LIKERT_SECTIONS` / `pages()` — `src/scoring.js`）。

### Work Style 4軸

| 軸 | 記号 | 一方の極 | 他方の極 |
|---|---|---|---|
| 視座 | P / B | **精密** ディテール・数値の正確性志向 | **俯瞰** 全体構造・ビジネス文脈志向 |
| 推論 | V / X | **検証** 根拠・基準への当てはめ重視 | **探索** 仮説形成・未知の論点重視 |
| 進め方 | S / A | **構造** 計画・標準化・段取り重視 | **適応** 即応・走りながらの調整重視 |
| 作業様式 | D / C | **深掘** 独立・単独深掘り | **協働** 対話・共同作業 |

4軸の極の組合せで16のアーキタイプが決まります。アーキタイプは**Work Styleのみ**から生成し、他のレイヤーは混入させません。

### スコアリング

- **Likert**: とても+2 / やや+1 / どちらとも0 / あまり−1 / まったく−2。逆転項目は符号反転
- **欠測**: 「経験がなく判断できない」（N/A）と未回答は欠測として平均から除外する。**0点にしない**
- **Personality**: 特性ごとの有効回答平均 m → `round((m+2)/4×100)`。有効回答3問未満の特性は「判定不能」としてレイヤーごと非表示
- **Work Style**: 軸ごと4問を極方向に合計 s（−8..+8）→ `pct = round(50 + 50|s|/8)`。s≥0で左極。s=0は既定極 + 「両極型」注記
- **Study**: 各指標1問。内部では `(v+2)/4×100` を持つが、1問ぶんの情報量に見合わないため**表示はスコアではなく3段タグ**（≥75 いまの学習スタイル / 50 ときどき / ≤25 これから試せる）
- **Subject DNA**: 上位3科目を、選ばれた設問（S1 苦にならない / S2 面白い / S3 いまも語れる）のタグで表示。並び順にのみ内部の重み（S1+2 / S2+3 / S3+2）を使い、**数値は表示しない**
- **Practice DNA**: 上位4領域をタグで表示（コア=好き×やりたい、フロンティア=未経験×やりたい、ほかは 好き / やりたい / 経験あり）。並び順にのみ内部の重み（P2+2 / P3順位3・2・1）を使う。**P1（経験）は加点しない**（経験の有無を適性として扱わないため）

実装は [`src/scoring.js`](src/scoring.js) に集約しています。

---

## 画面

| 画面 | 内容 |
|---|---|
| トップ | ヒーロー + キャラクターの流れる帯（装飾・押せない） + 5レイヤーカード + PRIVACYパネル + disclaimer |
| 質問 | STEP 1 コア9ページ（41問）→ STEP 2 任意9ページ（15回答）。進捗バー・途中保存・N/A対応 |
| 結果 | アーキタイプのキャラクター + 回答済みレイヤー + 日々の傾向 + Profile Map + 解説 + 画像保存 |
| アーキタイプ図鑑 | 16種の一覧。カードから個人スコアなしのプレビューを表示 |

`prefers-reduced-motion: reduce` ですべてのアニメーションを無効化します（トップの流れる帯は手でスクロールできる形に切り替わります）。選択状態は色だけでなく ✓ / 順位数字 / `aria-pressed` を併用します。

---

## アーキタイプのキャラクター

16のアーキタイプにはそれぞれ動物のキャラクターがいます。診断後の結果画面、図鑑カード、トップの流れる帯、ダウンロードするPNGシートで使用します。

| Code | アーキタイプ | 動物 | Code | アーキタイプ | 動物 |
|---|---|---|---|---|---|
| PVSD | 精密検証官 | ハリネズミ | BVSD | ナレッジビルダー | リス |
| PVSC | 品質ガーディアン | シェパード | BVSC | チームコーディネーター | コーギー |
| PVAD | 数字の探偵 | フクロウ | BVAD | 事業解像アナリスト | タカ |
| PVAC | 決算オペレーター | ライオン | BVAC | 対話型コンサルタント | オウム |
| PXSD | 基準設計士 | ツル | BXSD | 論点ハンター | キツネ |
| PXSC | 仕組み化アーキテクト | クモ | BXSC | クロスドメイン型 | カメレオン |
| PXAD | 独立深掘り職人 | モグラ | BXAD | 仮説ドリブン探索者 | イルカ |
| PXAC | 業務改善エンジニア | ビーバー | BXAC | 変革プロトタイパー | ネコ |

```text
assets/archetypes/{CODE}.jpg         720px  結果画面・PNGシート用
assets/archetypes/thumb/{CODE}.jpg   320px  図鑑カード・トップの帯用
```

**画像は差し替え・追加のみで反映されます。** コードは実在するファイルだけを使い、無いものは自動でグラデーション表示にフォールバックします（トップの帯からも除外されます）。ファイル名を上記の規則に合わせて置くだけで、`src/` の変更は不要です。

> **未提供: `PVAD`（数字の探偵 / フクロウ）**
> この1点だけ画像が未生成です。現在は結果画面・図鑑ともグラデーション表示になり、トップのマーキーからは除外され、`t/PVAD/` は `og:image` なしで生成されます。`assets/archetypes/PVAD.jpg`（720×720）と `assets/archetypes/thumb/PVAD.jpg`（320×320）を追加して `node scripts/build-archetype-pages.mjs` を再実行すれば、コード変更なしですべて表示されます。

元画像は1枚あたり約2.3MB・16枚で約33MBあるため、リポジトリには収めず、720px / 320px のJPEGに変換して同梱しています（合計約2.3MB）。各アーキタイプの `prompt`（`src/data.js`）は画像生成時のコンセプトとして保持しています。

---

## 構成

```text
├─ index.html               エントリ（ビルドなし）
├─ src/
│  ├─ app.js                画面切替・共有リンク復元
│  ├─ data.js               設問・軸・アーキタイプの全データ
│  ├─ scoring.js            スコアリング（設計書 §8 準拠）
│  ├─ state.js              状態と下書きの一時保存
│  ├─ export.js             PNG プロフィールシートの生成
│  ├─ ui.js                 最小限のDOMヘルパー
│  └─ screens/              home / quiz / result / types
├─ styles/app.css           Product UI Layer（トークン上書きを含む）
├─ t/{CODE}/index.html      アーキタイプ別ページ（16枚・生成物。共有リンクの着地先）
├─ assets/archetypes/       アーキタイプのキャラクター画像（720px / thumb 320px）
├─ design-system/           norimaki-tool-standards の design/ を複製
└─ scripts/
   ├─ dev-server.mjs             ローカル確認用の静的サーバー
   └─ build-archetype-pages.mjs  t/{CODE}/ を生成する
```

### アーキタイプ別ページ（`t/{CODE}/`）と SNS カード

結果は URL のハッシュ（`#p=…`）に入れています。ハッシュは HTTP リクエストに含まれず、
X のクローラーは JavaScript も実行しないため、**1枚の `index.html` では結果ごとに
`og:image` を変えられません**。GitHub Pages はクエリ文字列でも配信を切り替えられないので、
変えられるのはパスだけです。そこでアーキタイプ単位で16ページを事前生成しています。

```bash
node scripts/build-archetype-pages.mjs
```

- 各ページは `og:image` にそのアーキタイプのキャラクター画像（720×720）を持ち、`twitter:card` は `summary`。正方形サムネイルとして切り抜かれずに表示されます（`summary_large_image` は 1.91:1 に中央切り抜きされるため使っていません）
- 本体は同じ `src/app.js` を読み込みます。`#p=…` があれば共有された Work Style を復元し、なければそのアーキタイプの紹介として開きます
- 画像パスは `import.meta.url` 基準で解決するため、どの階層のページからでも同じ `src/` が使えます
- 画像が未用意のアーキタイプでは `og:image` を出しません（404 を指すと壊れたカードになるため）
- **`src/data.js` のアーキタイプや画像を変えたら再実行してコミットしてください**

`SITE`（公開URL）は生成スクリプト冒頭の定数です。独自ドメインに移す場合はそこだけ変えます。

`design-system/` は [norimaki-audit/norimaki-tool-standards](https://github.com/norimaki-audit/norimaki-tool-standards) の `design/` をそのまま持ち込んだBrand Layerです。アプリ固有の紙色地・深緑・角丸2pxへの上書きは `styles/app.css` の `:root` に集約しており、design-system 側のファイルは変更していません（更新時は上書きコピーで追従できます）。

---

## デプロイ

`main` ブランチのルートを GitHub Pages がそのまま配信します。CI・環境変数はありません。
唯一の生成物は `t/{CODE}/`（アーキタイプ別ページ）で、これはコミット済みなので
デプロイ時のビルドは不要です。`src/data.js` を変えたときだけ再生成してください。

`package.json` は依存パッケージを持たず、`scripts/` の Node スクリプトから
`src/*.js` を ES モジュールとして読むために `"type": "module"` を宣言しているだけです。

---

## 注意事項

本サービスは心理学研究（IPIP等の公開心理尺度）を参考にした**独自の自己理解コンテンツ**です。妥当性検証済みの心理検査ではありません。

- 心理検査・能力検査・採用・人事評価を目的としたものではありません
- 適職判定・求人推薦・年収予測は行いません
- Work Style 4軸はプロダクト独自の軸であり、確立した心理尺度ではありません
- Study Behavior は各指標1問のため、0–100 のスコアやバーでは出しません。「当てはまる／どちらともいえない／当てはまらない」の3段タグとして表示し、当てはまらない側も欠落ではなく「これから試せる」として置いています
- Subject / Practice DNA も数値では出しません。選択操作の集計であって測定値ではないため、「なぜその科目・領域が挙がったか」（苦にならない／面白い／いまも語れる、コア／フロンティア／経験あり）をタグで示します
- 設問・アーキタイプ名・解説・画像コンセプトはすべてオリジナルです。MBTI® の設問・タイプ名称・4文字コードは使用していません

### 注意書きの出し方

「これが無いと利用者が結果を誤解するか」だけを基準に置き、繰り返しと言い換えは削っています。

| 残す（誤解の実害がある） | 置き場所 |
|---|---|
| 心理検査・採用評価・適職判定ではない | トップと結果の最下部に1文ずつ |
| アーキタイプは Work Style から作るラベルである | 結果ヘッダーのボタン下に1行 |
| 結果は保存されない・閉じると取り出せない | 結果上部の保存行に1行 |
| Personality の高低に良し悪しはない／Study は各指標1問／DNA は点数ではない | 各カードの見出し脇に短く |
| コア=好き×やりたい、フロンティア=未経験×やりたい | Practice DNA の見出し脇（タグを読むのに必要） |

畳んだもの: トップの PRIVACY 詳細（`<details>`）。
削ったもの: 結果下部の再掲パネルの本文（ボタンだけ残す）、各カードの本文リード、保存パネルの長い補足。

象徴ビジュアルは現在CSSグラデーションのプレースホルダで、各アーキタイプが画像生成用プロンプト（`prompt`）と2色（`c`）を保持しています。将来、生成画像に差し替えられる設計です。
